# ----------------------------
# Database Configuration
# ----------------------------
# Replace <username>, <password>, <host>, <port>, <dbname> with real values.
DATABASE_URL=postgresql://<username>:<password>@<host>:<port>/<dbname>

# ----------------------------
# JWT / Authentication
# ----------------------------
# SECRET_KEY must be long and random (use `openssl rand -hex 32`)
SECRET_KEY=change-me-in-prod-very-secret-and-long-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional claims if you add B2C later
JWT_ISSUER=<your-issuer-url>
JWT_AUDIENCE=<your-audience>

# ----------------------------
# Azure Storage (if needed for docs/PDFs)
# ----------------------------
AZURE_STORAGE_ACCOUNT_NAME=<storage-account-name>
AZURE_STORAGE_ACCOUNT_KEY=<storage-account-key>
AZURE_BLOB_CONTAINER=<container-name>

# ----------------------------
# Other service integrations
# ----------------------------
# Add keys for APIs, Cognitive Services, etc. as needed.
