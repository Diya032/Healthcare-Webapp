# ----------------------------
# Database Configuration
# ----------------------------
# SQLite for local dev:
# DATABASE_URL=sqlite:///./patients.db
#
# Azure SQL (SQLAlchemy format for SQL Server):
# DATABASE_URL=mssql+pyodbc://<username>:<password>@<server>.database.windows.net:1433/<dbname>?driver=ODBC+Driver+17+for+SQL+Server

DATABASE_URL=sqlite:///./patients.db

# ----------------------------
# JWT / Authentication
# ----------------------------
# SECRET_KEY must be long and random (use `openssl rand -hex 32`)
SECRET_KEY=change-me-in-prod-very-secret-and-long-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional claims if you add B2C later
JWT_ISSUER=<your-issuer-url>
JWT_AUDIENCE=<your-audience>

# ----------------------------
# Azure Storage (if needed for docs/PDFs)
# ----------------------------
AZURE_STORAGE_ACCOUNT_NAME=<storage-account-name>
AZURE_STORAGE_ACCOUNT_KEY=<storage-account-key>
AZURE_BLOB_CONTAINER=<container-name>

# ----------------------------
# Other service integrations
# ----------------------------
# Add keys for APIs, Cognitive Services, etc. as needed.


# Which environment (development or production)
ENV=development

# In dev, we usually want localhost + ngrok
DEV_CORS=https://ab37c26da88c.ngrok-free.app

# In prod, only your real frontend domain
PROD_CORS=https://myfrontend.com
